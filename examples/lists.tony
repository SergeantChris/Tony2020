def main ():
  def list[int] qsort (list[int] l):
    int seed, i
    list[int] l

    for i := 0, l := nil; i < 16; i := i+1:
      seed := (seed * 137 + 220 + i) mod 101
      l := seed # l
    end

    return l
  end

  list[int] l
  l := nil
  a := qsort(l)
end
