def main ():
  char seed
  decl list[int] qsort (char q; int a)
  %char[] marilena
  int i
  list[int] l

  def list[int] qsort (char q; int a):
    int seed
		list[int] l
    seed := 1
    l := nil
    l := seed # l
    return l
  end

  seed := 'a'
  i := 1+1
  l := nil
  %"cute"[0] := 'x'
  l := qsort(seed, i)

end
