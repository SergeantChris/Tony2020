def simple ():
  def bool prime? (int n; ref int l):
    n := n+2
    n := l + n
    return true
  end

  int limit, number, counter
  bool c
  counter := 0
  limit := 10
  c:=prime?(counter, limit)
  number:= limit + 1
end
